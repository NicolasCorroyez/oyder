# Navigation (GoRouter)

Routes:

- `/login`
- `/` → Dashboard (tabs)
  - `/calendar`
  - `/places`
  - `/creators`
  - `/orders`
- `/orders/new`
- `/orders/:id`
- `/orders/:id/edit`

Redirections:

- Si pas de seller connecté → `/login`.
